# DiplomaEZ

A localhost browser study game with WASD movement, topic portals, random diploma questions, answer review, and saved progress.

## Run locally

Open the project folder and start a local static server. For example:

```bash
python3 -m http.server 4177
```

Then open `http://localhost:4177`.

On Windows, you can also run `run_localhost.bat`.
